function add(num1,num2=26) { //num2 = 26 is the default parameter
    return num1 + num2;
}

const total = add(20); //parameter ekta kom er jonno output NAN aslo...but default parameter use korle ta asbe na
console.log(total);